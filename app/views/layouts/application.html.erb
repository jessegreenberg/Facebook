<!DOCTYPE html>
<html>
	<body>
		<%= render 'layouts/title' %>
		<div class="container" >
			<% flash.each do |key, value| %>
				<div class="alert alert-<%= key %>"><%= value %></div>
			<% end %>
			<%= render 'layouts/header' %>
			<%= yield %>
		</div>
	</body>
</html>
