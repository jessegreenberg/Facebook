
<% unless current_user?(@user) %>
	<div id="follow_form">
		<% if current_user.friends_with?(@user) %>
			<%= render 'remove_friend' %>
		<% else %>
			<%= render 'add_friend' %>
		<% end %>
	</div>
<% end %>
