<% provide(:pagename, "#{@possessive_name} Newsfeed") %>

<% if signed_in? %>
	<div class ="row">
		<div class="span8">
			<h3> Newsfeed </h3>
			<%= render 'shared/userpost_form' %>
			<%= render 'shared/newsfeed' %>
		</div>
	</div>
<% end %>
