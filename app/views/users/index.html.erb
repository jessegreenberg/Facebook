<% provide(:pagename, 'Friends') %>
<h1>
	Friends
</h1>

<%= will_paginate %>
<u1 class="users">
	<% @users.each do |user| %>
		<li>
			<%= link_to user.name, user %>
		</li>
	<% end %>
</u1>

<%= will_paginate %>
