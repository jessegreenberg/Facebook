<% provide(:pagename, @user.name) %>
<div class="row">

	<h1><%= @user.name %></h1>
	
	<div class="span8">
		<% if @user.userposts.any? %>
			<h3> User Posts (<%= @user.userposts.count %>)</h3>
			<ol class="userposts">
				<%=render @userposts %>
			</ol>
			<%= will_paginate @userposts %>
		<% end %>
	</div>
</div>
